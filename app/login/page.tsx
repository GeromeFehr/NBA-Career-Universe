export default async function Page({searchParams}:{searchParams:Promise<{error?:string}>}){const q=await searchParams;return <div className="login"><span className="eyebrow">CONTROL ROOM</span><h1>Login</h1><p className="muted">Nur für die Bearbeitung deiner Karriere.</p>{q.error&&<p className="loginError">Passwort falsch.</p>}<form action="/api/auth/login" method="post"><label>Admin-Passwort<input name="password" type="password" autoFocus/></label><button>Anmelden</button></form></div>}
