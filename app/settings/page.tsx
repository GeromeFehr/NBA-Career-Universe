import {hasDatabaseConfig} from "@/lib/db";
export default function Page(){return <><div className="sectionHead"><div><span className="eyebrow">SYSTEM</span><h1>Setup & Architektur</h1></div></div><div className="grid2"><div className="card"><h2>Datenbank</h2><p>Supabase Postgres · {hasDatabaseConfig()?"Environment erkannt":"noch nicht konfiguriert"}</p><p className="muted">Die Daten bleiben über Teamwechsel und Saisons erhalten. Mutationen laufen ausschließlich serverseitig.</p></div><div className="card"><h2>KI</h2><p>OpenAI Responses API</p><p className="muted">Mit OPENAI_API_KEY entsteht echte variierende Berichterstattung. Ohne Key greift ein lokaler Fallback.</p></div><div className="card"><h2>Schedule</h2><p>NBA CDN → Fixture-Fallback → manueller CSV-Import</p><p className="muted">Damit hängt die Karriere nicht an genau einer Quelle.</p></div><div className="card"><h2>Hosting</h2><p>Netlify / Vercel</p><p className="muted">Secrets ausschließlich als Environment Variables setzen, niemals committen.</p></div></div></>}
