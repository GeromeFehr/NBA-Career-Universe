import {pageContext} from "@/lib/universe";import MediaCard from "@/components/MediaCard";export const dynamic="force-dynamic";
export default async function Page(){const {client,career,universe}=await pageContext();const {data}=await client.from("media_posts").select("*").eq("career_id",career.id).order("created_at",{ascending:false}).limit(200);return <><div className="sectionHead"><div><span className="eyebrow">SOCIAL FEED · {universe.name}</span><h1>Timeline</h1></div></div><div className="mediaStack">{(data||[]).filter((x:any)=>x.kind==="social"||x.kind==="wildcard").map((p:any)=><MediaCard key={p.id} post={p}/>)}</div></>}
