import {isAdmin} from "@/lib/auth";import AdminConsole from "@/components/AdminConsole";import Link from "next/link";export const dynamic="force-dynamic";
export default async function Page(){if(!(await isAdmin()))return <div className="login"><span className="eyebrow">PRIVATE AREA</span><h1>Control Room</h1><p className="muted">Für Spielstände, Stats, Trades und Story-Events anmelden.</p><Link className="buttonLink" href="/login">Login</Link></div>;return <><div className="sectionHead"><div><span className="eyebrow">ADMIN</span><h1>Career Control Room</h1></div><form action="/api/auth/logout" method="post"><button>Logout</button></form></div><AdminConsole/></>}
