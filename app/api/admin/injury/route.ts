import {apiFailure} from "@/lib/http";
import {NextResponse} from "next/server";import {requireAdmin,apiStatus} from "@/lib/auth";
export async function POST(req:Request){try{const {career,client}=await requireAdmin();const b=await req.json();const {error}=await client.from("injuries").insert({career_id:career.id,start_date:b.startDate||career.universe_date,injury:b.injury,severity:b.severity||"unknown",status:b.status||"active"});if(error)throw error;await client.from("career_events").insert({career_id:career.id,event_date:b.startDate||career.universe_date,event_type:"injury",title:"Injury update",description:b.injury,metadata:{severity:b.severity}});return NextResponse.json({ok:true})}catch(e){return apiFailure(e)}}
