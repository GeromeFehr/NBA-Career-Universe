import {NextResponse} from "next/server";import {requireAdmin,apiStatus} from "@/lib/auth";import {generateTradeMarket} from "@/lib/ai";
export async function POST(){try{const {career}=await requireAdmin();const offers=await generateTradeMarket(career.id);return NextResponse.json({ok:true,offers,message:`${offers.length} Angebote erzeugt`})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:String(e)},{status:apiStatus(e)})}}
