import {NextResponse} from "next/server";import {requireAdmin,apiStatus} from "@/lib/auth";
export async function POST(req:Request){try{const {career,client}=await requireAdmin();const b=await req.json();const {data:s}=await client.from("seasons").select("id").eq("current",true).single();if(!s)throw new Error("Current season not found");const {error}=await client.from("award_snapshots").insert({career_id:career.id,season_id:s.id,award:b.award,rank:b.rank,score:b.score||null,as_of_date:b.asOfDate,note:b.note||null});if(error)throw error;return NextResponse.json({ok:true})}catch(e){return NextResponse.json({error:e instanceof Error?e.message:String(e)},{status:apiStatus(e)})}}
