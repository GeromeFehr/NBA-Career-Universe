import {NextResponse} from "next/server";import {requireAdmin} from "@/lib/auth";import {db} from "@/lib/db";
export async function POST(req:Request){try{await requireAdmin();const b=await req.json(),client=db();const {data:c}=await client.from("career_profiles").select("*").limit(1).single();if(!c)throw new Error("Career profile not found");const {error}=await client.from("career_events").insert({career_id:c.id,event_date:b.date||c.universe_date,event_type:b.type||"other",title:b.type||"Story event",description:b.description});if(error)throw error;return NextResponse.json({ok:true})}catch(e:any){return NextResponse.json({error:e.message},{status:e.message==="UNAUTHORIZED"?401:500})}}
