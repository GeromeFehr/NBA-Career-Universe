import {NextResponse} from "next/server";import {requireAdmin} from "@/lib/auth";import {db} from "@/lib/db";
export async function POST(req:Request){try{await requireAdmin();const b=await req.json();if(!/^\d{4}-\d{2}-\d{2}$/.test(b.date))return NextResponse.json({error:"Ungültiges Datum"},{status:400});const client=db();const {data:c}=await client.from("career_profiles").select("id").limit(1).single();await Promise.all([client.from("career_profiles").update({universe_date:b.date}).eq("id",c.id),client.from("world_settings").update({universe_date:b.date,updated_at:new Date().toISOString()}).eq("career_id",c.id)]);return NextResponse.json({ok:true})}catch(e:any){return NextResponse.json({error:e.message},{status:e.message==="UNAUTHORIZED"?401:500})}}
