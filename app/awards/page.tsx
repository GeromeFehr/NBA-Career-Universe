import {db} from "@/lib/db";export const dynamic="force-dynamic";
export default async function Page(){const client=db();const {data:c}=await client.from("career_profiles").select("*").limit(1).single();if(!c)return <div className="card">Keine Karriere gefunden.</div>;const [{data:a},{data:m}]=await Promise.all([client.from("award_snapshots").select("*").eq("career_id",c.id).order("as_of_date",{ascending:false}),client.from("milestones").select("*").eq("career_id",c.id).order("achieved_at",{ascending:false})]);const latest=new Map<string,any>();for(const x of a||[])if(!latest.has(x.award))latest.set(x.award,x);return <><div className="sectionHead"><div><span className="eyebrow">RACE WATCH</span><h1>Awards & Records</h1></div></div>{Array.from(latest.values()).map((x:any)=><div className="card awardCard" key={x.id}><div><span className="eyebrow">{x.as_of_date}</span><h2>{x.award}</h2><p className="muted">{x.note}</p><div className="progress"><i style={{width:`${Math.max(8,100-Math.min(9,x.rank-1)*10)}%`}}/></div></div><div className="rank">#{x.rank}</div></div>)}<div className="sectionHead"><h2>Milestones</h2></div><div className="offerGrid">{(m||[]).map((x:any)=><div className="card" key={x.id}><b>{x.title}</b><p>{x.description}</p></div>)}</div></>}
